fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.11.0,<3.0.0
pydantic-settings==2.7.0
sqlmodel==0.0.39
pygithub==2.9.1
claude-agent-sdk==0.2.136
pyjwt[crypto]==2.13.0
pyyaml==6.0.2
httpx==0.28.1
rich==15.0.0
loguru==0.7.3
python-dotenv==1.0.1

# Multi-agent architecture (Dimension 2) + Slack/Discussions Q&A assistant
slack-bolt==1.30.0        # events adapter (mounted into FastAPI) + AI Assistant surface
neo4j==6.2.0              # shared graph store — src/graph.py
lightrag-hku==1.5.6       # incremental, version-tagged doc indexing + hybrid retrieval,
                           # configured with graph_storage="Neo4JStorage" (see tools/doc_retriever.py)
anthropic==0.122.0        # LightRAG's llm_model_func (lightrag.llm.anthropic.anthropic_complete)
voyageai==0.5.0           # LightRAG's embedding_func (lightrag.llm.voyageai.voyageai_embed) —
                           # Anthropic has no embeddings endpoint of its own
markdownify==1.2.3        # HTML -> markdown for scripts/crawl_docs.py (no browser needed;
                           # kyverno.io is server-rendered — see that script's docstring)
certifi==2026.7.22         # already a transitive dep (requests/httpx), pinned directly here
                           # because main.py/build_doc_index.py/src/memory.py now read
                           # certifi.where() explicitly to fix aiohttp's SSL trust store —
                           # see the comment next to each os.environ.setdefault("SSL_CERT_FILE", ...) call

# Dimension 3 — Graphiti temporal memory (src/memory.py), on the same Neo4j
# instance as the doc graph above. No new provider deps: graphiti-core's
# optional `anthropic`/`voyageai` extras are already satisfied by the
# anthropic/voyageai pins above, so the plain package is enough here.
graphiti-core==0.29.3
